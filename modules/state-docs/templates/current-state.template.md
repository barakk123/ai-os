# Current State - <project>  (AI-facing, detailed)

> AI-facing detailed state. Companion to PROJECT_STATUS.md (owner-facing, concise) + HANDOFF.md
> (live warm-start) + docs/TRACKER.md (deferrals/findings) + MEMORY.md (index).
> Rewritten lean <date>. Full prior history is preserved verbatim in
> docs/archive/current-state-<date>.md. Keep this file lean; roll a dated snapshot to the archive
> when it grows. This doc describes reality - it never overrides product truth (spec wins on conflict).

**Last updated:** <date> - <one-line "what changed this session">

---

## What is built

- <the present capability set - include the AI-layer artifacts the bootstrap produced>
- <...>

## What is pending

- <concrete, with enough detail that an implementer could pick it up cold>
- <...>

## Decisions / constraints (locked)

> The rulings that must NOT be re-litigated. Each as one line: the decision -> its rationale, or the
> field/mechanism it pins. This is the highest-value section - it is where "we already decided X,
> do not re-invent it" lives durably.

- <decision> -> <rationale / the field or mechanism it pins>
- <...>

## Blockers / required user actions

- Blockers: <none / what is blocked and why>
- Required user actions: <none / pointer to the tracker's USER_ACTION rows (the tracker is the registry)>

---

<!--
Deferrals + findings are NOT listed here in prose - they live in docs/TRACKER.md (T-XXXX rows).
This doc points at the tracker; it does not become a second copy of the ledger.
Durable lessons -> a memory topic file (referenced from MEMORY.md), not here.
Headline counts are computed roll-ups (re-derive on update), never hand-typed totals.
-->
