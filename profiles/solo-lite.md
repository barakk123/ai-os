# Profile: solo-lite

> The Universal Core only. The collaboration OS with zero heavy machinery.

**For:** a small or personal project, a script, an experiment - anywhere you want the shared
language, memory discipline, handoff/sync, state docs, tracker, and gates, but nothing more.

## Modules
Core (all): `shared-language` · `mutual-push` · `handoff` · `sync` · `memory` · `state-docs` ·
`tracker` · `doc-hygiene` · `platform-notes` · `gates` · `git-dev-push` *(deferred - not yet built; the commit step falls back to a plain commit)*

<!-- ai-os:profile
core: [shared-language, mutual-push, handoff, sync, memory, state-docs, tracker, doc-hygiene, platform-notes, gates]
deferred: [git-dev-push]
excluded: [source-of-truth, test-program, agent-fleet]
-->

## Excludes (add a-la-carte if wanted)
- `source-of-truth` - add it (any mode) when the project gains a real source of truth.
- `test-program` - add it (lite) when you start testing.
- `agent-fleet` - add it (lite) when you want a review gate.
